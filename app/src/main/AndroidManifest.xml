<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.clnain"> <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".smartfactory.tools.SmartFactoryApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name" android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <activity android:name=".SplashActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"
            android:label="@string/main_page"
            android:icon="@mipmap/icon_lanuncher"/>

        <activity android:name=".SettingActivity"
            android:label="@string/global_param_setting"/>

        <activity
            android:name=".ChartActivity"
            android:label="@string/chart_activity_label" />
    </application>

</manifest>